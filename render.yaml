services:
  - type: web
    name: downloader-bot
    env: go
    plan: free
    autoDeploy: true
    buildCommand: go build -o main .
    startCommand: ./main
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: PORT
        value: 10000
